
Vue.component('molds-component', {
    props: ['userData'],
    data() {
        return {
            showDialog: false,
            newMold: this.getInitialMoldData(),
        };
    },
    template: `

    `,
    methods: {
        
    }
});
